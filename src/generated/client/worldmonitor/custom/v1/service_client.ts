// @ts-nocheck
// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/custom/v1/service.proto

export interface ListPlaneTestPointsRequest {}

export interface ListPlaneTestPointsResponse {
  points: PlaneTestPoint[];
}

export interface PlaneTestPoint {
  id: string;
  latitude: number;
  longitude: number;
  name: string;
  type: string;
}

export interface CustomServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface CustomServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class CustomServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: CustomServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, '');
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async listPlaneTestPoints(
    req: ListPlaneTestPointsRequest,
    options?: CustomServiceCallOptions,
  ): Promise<ListPlaneTestPointsResponse> {
    const path = '/api/custom/v1/list-plane-test-points';
    const url = this.baseURL + path;

    const headers: Record<string, string> = {
      'Content-Type': 'application/json',
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: 'GET',
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return (await resp.json()) as ListPlaneTestPointsResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = 'ApiError';
    this.statusCode = statusCode;
    this.body = body;
  }
}
